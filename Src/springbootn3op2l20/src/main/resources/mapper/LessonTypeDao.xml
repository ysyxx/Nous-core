<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.LessonTypeDao">

    <resultMap type="com.entity.LessonTypeEntity" id="LessonTypeMap">
        <result property="lessonType" column="lesson_type"/>
    </resultMap>

    <select id="selectListVO"
            resultType="com.entity.vo.LessonTypeVO" >
        SELECT lesson_type.* FROM lesson_type lesson_type
        <where> 1=1 ${ew.sqlSegment}</where>
    </select>

    <select id="selectVO"
            resultType="com.entity.vo.LessonTypeVO" >
        SELECT lesson_type.* FROM lesson_type lesson_type
        <where> 1=1 ${ew.sqlSegment}</where>
    </select>

    <select id="selectListView"
            resultType="com.entity.view.LessonTypeView" >
        SELECT lesson_type.* FROM lesson_type lesson_type
        <where> 1=1 ${ew.sqlSegment}</where>
    </select>

    <select id="selectView"
            resultType="com.entity.view.LessonTypeView" >
        SELECT lesson_type.* FROM lesson_type lesson_type <where> 1=1 ${ew.sqlSegment}</where>
    </select>

</mapper>
